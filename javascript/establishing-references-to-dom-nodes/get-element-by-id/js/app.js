window.onload = () => {
    let firstParagraph = document.getElementById(`first-paragraph`);
    firstParagraph.textContent = `This <em>is</em> the first paragraph`;
    // firstParagraph.innerHTML = `This <em>is</em> the first paragraph`;
};
